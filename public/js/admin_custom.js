$(function() {
    /*i = $('#userList').text().indexOf('(') + 1;
    j = $('#userList').text().indexOf(')');
    //console.log($('#userList').text().substring(i, j));
    $.get( "/admin/usuarios/contar", function( data ) {
        //alert( "Data Loaded: " + data );
        $replaceVal = $('#userList').text().substring(i, j);
        $('#userList')[0].text($('#userList').text().replace($replaceVal, data))
    });*/
});
